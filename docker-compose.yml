services:
  moonbunny-host:
    build: .
    ports:
      - '${PORT:-8080}:8080'
    environment:
      DEPLOY_TOKENS: ${DEPLOY_TOKENS:-changeme} # comma-separated
    volumes:
      - '${DATA_PATH:-./data}:/data'
    restart: unless-stopped
