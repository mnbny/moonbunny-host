name: moonbunny-host-test

services:
  app:
    build: ..
    environment:
      DEPLOY_TOKENS: test-token

  proxy:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - '127.0.0.1:18199:80'
    depends_on:
      - app
