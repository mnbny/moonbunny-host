events {}

http {
  server {
    listen 80;

    location / {
      # Mirrors Nginx Proxy Manager: proxy everything, allow deploy-sized bodies.
      client_max_body_size 50m;
      proxy_pass http://app:8080;
      proxy_set_header Host $host;
    }
  }
}
